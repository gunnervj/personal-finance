# Personal Finance Application Configuration - Server Deployment
# Example configuration for rudra.core server with subdomain routing

# ============================================================================
# SUBDOMAIN-BASED DEPLOYMENT (Recommended for Production)
# ============================================================================
# This configuration uses subdomains for clean URLs
# Requires reverse proxy (nginx/traefik) to route traffic
#
# DNS Setup Required:
#   app.rudra.core -> server IP
#   keycloak.rudra.core -> server IP
#   api.rudra.core -> server IP
# ============================================================================

# Application URLs (Subdomain-based)
FRONTEND_URL=http://app.rudra.core
KEYCLOAK_URL=http://keycloak.rudra.core
USER_SERVICE_URL=http://api.rudra.core:8081
BUDGET_SERVICE_URL=http://api.rudra.core:8082
TRANSACTION_SERVICE_URL=http://api.rudra.core:8083

# Keycloak Proxy Configuration
# Required when Keycloak is behind a reverse proxy
KC_HOSTNAME=keycloak.rudra.core
KC_PROXY=edge

# SSL/TLS Configuration
# Set to '0' for self-signed certificates (development/internal networks only)
# Remove or leave empty for production with valid SSL certificates
NODE_TLS_REJECT_UNAUTHORIZED=

# Database Configuration
POSTGRES_DB=personalfinance
POSTGRES_USER=admin
POSTGRES_PASSWORD=change-me-in-production

# Keycloak Admin Configuration
KEYCLOAK_ADMIN_USER=admin
KEYCLOAK_ADMIN_PASSWORD=change-me-in-production

# NextAuth Secret (MUST change in production - minimum 32 characters)
NEXTAUTH_SECRET=change-me-to-random-32-character-string-in-production
