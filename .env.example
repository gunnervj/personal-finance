# Personal Finance Application Configuration
# Copy this file to .env and modify the values as needed

# ============================================================================
# DEPLOYMENT MODE: Choose your deployment pattern
# ============================================================================
#
# MODE 1: Localhost with Ports (Development)
# -------------------------------------------
# FRONTEND_URL=http://localhost:3000
# KEYCLOAK_URL=http://localhost:8080
# USER_SERVICE_URL=http://localhost:8081
# BUDGET_SERVICE_URL=http://localhost:8082
# TRANSACTION_SERVICE_URL=http://localhost:8083
#
# MODE 2: Subdomain-based (Production)
# -------------------------------------
# FRONTEND_URL=http://app.example.com
# KEYCLOAK_URL=http://keycloak.example.com
# USER_SERVICE_URL=http://api.example.com/user
# BUDGET_SERVICE_URL=http://api.example.com/budget
# TRANSACTION_SERVICE_URL=http://api.example.com/transaction
#
# MODE 3: Single Domain with Ports (Server without reverse proxy)
# ----------------------------------------------------------------
# FRONTEND_URL=http://server-ip:3000
# KEYCLOAK_URL=http://server-ip:8080
# USER_SERVICE_URL=http://server-ip:8081
# BUDGET_SERVICE_URL=http://server-ip:8082
# TRANSACTION_SERVICE_URL=http://server-ip:8083
#
# ============================================================================

# Application URLs
FRONTEND_URL=http://localhost:3000
KEYCLOAK_URL=http://localhost:8080
USER_SERVICE_URL=http://localhost:8081
BUDGET_SERVICE_URL=http://localhost:8082
TRANSACTION_SERVICE_URL=http://localhost:8083

# Keycloak Proxy Configuration (for reverse proxy deployments)
# KC_HOSTNAME: External hostname for Keycloak (e.g., keycloak.example.com)
# KC_PROXY: Proxy mode - edge (SSL termination) | reencrypt | passthrough | none
# Leave empty for localhost development
KC_HOSTNAME=
KC_PROXY=

# SSL/TLS Configuration
# Set to '0' ONLY for development with self-signed certificates
# Leave empty or remove for production with valid SSL certificates
NODE_TLS_REJECT_UNAUTHORIZED=

# Database Configuration
POSTGRES_DB=personalfinance
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin

# Keycloak Admin Configuration
KEYCLOAK_ADMIN_USER=admin
KEYCLOAK_ADMIN_PASSWORD=admin

# NextAuth Secret (MUST be changed in production - minimum 32 characters)
NEXTAUTH_SECRET=dev-secret-change-in-production-min-32-chars
